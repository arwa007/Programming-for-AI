#Write a Python function to check if the last letter of user input string is a vowel or a consonant.

def check_last_letter():
    user_input = input("Enter your string:\n").strip()
    
    if user_input[-1].lower() in ['a', 'e', 'i', 'o', 'u']:
        print("Ends with a vowel")
    else:
        print("Ends with a consonant")

check_last_letter()
