#Write a program to make function employee() meeting following requirements:
#Employee name and monthly salary will be passed to this function. This function will cut 2 percent tax from salary and display salary after tax with name of employee. 
#If the salary is missing in the function call then assign default value 10,000 to salary.

def employee(name, salary=10000):
    print(f"The employee name is {name}. The salary after tax deduction is {salary*0.98}.")

username = input("Enter Employee Name: ")
userSalary = float(input("Enter Employee Salary: "))
employee(name=username, salary=userSalary)
